#include<stdio.h>
void main()
{
int l,b,area;
printf("Enter the values of l:");
scanf("%d",&l);
printf("Enter the value of b:");
scanf("%d", &b);
area=l*b;
printf("the area of rectangle is %d",area);
}