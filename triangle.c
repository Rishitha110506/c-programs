#include<stdio.h>
void main()
{
float b,h,area;
printf("Enter the value of b:");
scanf("%0.2f",&b);
printf("Enter the value of h:");
scanf("%0.2f",&h);
area=1/2* b* h;
printf("the area of triangle is %0.2f",area);
}